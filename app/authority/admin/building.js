'use strict'

/**
 * 楼宇
 */
module.exports = {
   create: {
      // where: ['uid'],
      // action(data) { }
   },
   find: {
      // where: ['uid'],
   },
   update: {
      // where: ['uid'],
   },
   destroy: {
      // where: ['uid'],
   },
}