'use strict'

/**
 * 管理员
 */
module.exports = {
   find: {
      where: ['uid'],
   },
   create: {
      where: ['uid'],
   },
   update: {
      where: ['uid'],
   },
   destroy: {
      where: ['uid'],
   },
}