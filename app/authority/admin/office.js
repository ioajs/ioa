'use strict'

/**
 * 写字楼
 */
module.exports = {
   find: {
      // where: ['uid'],
      // action(ctx) { }
   },
   create: {
      where: ['uid'],
      // action(ctx) { }
   },
   update: {
      // where: ['uid'],
      // action(ctx) { }
   },
   destroy: {
      // where: ['uid'],
      // action(ctx) { }
   },
}