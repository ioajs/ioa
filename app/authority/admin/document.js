'use strict'

/**
 * 角色
 */
module.exports = {
   find: {
      // where: ['uid'],
   },
   create: {
      // where: ['uid'],
   },
   update: {
      // where: ['uid'],
   },
   destroy: {
      // where: ['uid'],
   },
}