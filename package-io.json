{
   "main": {
      "enable": true,
      "config": {
         "mixin": {
            "a": 888
         }
      }
   },
   "db": {
      "enable": true
   },
   "admin": {
      "enable": true
   },
   "other": {
      "enable": true
   },
   "user": {
      "enable": true
   },
   "sequelize": {
      "enable": false,
      "package": "io-db",
      "config": {
         "postgres": {
            "host": "localhost",
            "database": "test",
            "username": "postgres",
            "password": "postgres"
         }
      },
      "env": {
         "localhost": {},
         "development": {},
         "production": {}
      }
   }
}